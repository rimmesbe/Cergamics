<div>
  <h1>Tile Categories</h1>
  <ul>
    <% @tile_categories.each do |category| %>
      <%= image_tag category.image.url(:square) %>
      <li><%= link_to category.title, tile_category_path(category)%></li>
    <% end %>
  </ul>
</div>